% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_analysis_project.R
\name{create_analysis_project}
\alias{create_analysis_project}
\title{Create a reproducible R analysis project}
\usage{
create_analysis_project(
  path,
  preset = NULL,
  mode = NULL,
  project_name = NULL,
  template = c("standard", "targets", "quarto"),
  dependency_profile = c("analysis", "minimal", "modelling", "geospatial",
    "package-development", "custom"),
  packages = NULL,
  code_loading = c("source", "package", "box"),
  use_quarto = TRUE,
  use_rmarkdown = FALSE,
  use_renv = TRUE,
  use_targets = FALSE,
  use_git = TRUE,
  use_config = TRUE,
  use_lintr = FALSE,
  use_styler = FALSE,
  use_pkgdown = FALSE,
  install_packages = FALSE,
  snapshot_renv = TRUE,
  run_health_check = TRUE,
  strict = FALSE,
  overwrite = FALSE,
  open = interactive()
)
}
\arguments{
\item{path}{Target project path. Relative paths are resolved against the
current working directory before scaffolding starts.}

\item{preset}{High-level project preset. Use \code{"analysis"} for a general
analyst project, \code{"modelling"} for model-heavy work, \code{"geospatial"} for
spatial analysis, \code{"pipeline"} for a targets-style workflow, and
\code{"package"} for package-development scaffolding. When omitted, the preset
is inferred from the detailed options.}

\item{mode}{Scaffold mode. Use \code{"simple"} for the minimal analyst-facing
scaffold and \code{"advanced"} for the full package-style scaffold. When
omitted, the mode is inferred from the requested options.}

\item{project_name}{Project name. Defaults to the basename of the resolved
\code{path}.}

\item{template}{Project template. Use \code{"standard"} for a general
script-and-report workflow, \code{"targets"} when the project should be driven
by a \href{https://docs.ropensci.org/targets/}{\code{targets}} pipeline, and
\code{"quarto"} when Quarto reporting is a core part of the scaffold.}

\item{dependency_profile}{Dependency preset used to choose a default package
set. Use \code{"minimal"} for a light scaffold, \code{"analysis"} for general data
wrangling and reporting, \code{"modelling"} for statistical or predictive work,
\code{"geospatial"} for spatial analysis, \code{"package-development"} when project
code should behave like an internal package, and \code{"custom"} when you want
to supply the package set yourself through \code{packages}.}

\item{packages}{Optional character vector of package names. Used when
\code{dependency_profile = "custom"} and otherwise treated as additional
packages to install and record on top of the selected profile.}

\item{code_loading}{Strategy for loading reusable project code. One of
\code{"package"}, \code{"box"}, or \code{"source"}. Use \code{"package"} for the most
structured workflow with package-style \verb{R/}, tests, and \code{pkgload};
\code{"box"} for modular code without package conventions; and \code{"source"} for
direct file sourcing. This mainly matters in advanced mode.}

\item{use_quarto}{Logical. Should Quarto project files and \code{.qmd} report
templates be created? In simple mode this creates \code{reports/main_report.qmd}
without exposing Quarto project infrastructure.}

\item{use_rmarkdown}{Logical. Should \code{.Rmd} report templates be created?
Enable this when your team still relies on R Markdown or needs both report
formats. This currently routes to the advanced scaffold.}

\item{use_renv}{Logical. Should \code{renv} be initialised if available? Enable
this for reproducible environments, especially for team projects or work
that will be rerun later.}

\item{use_targets}{Logical. Should a \code{targets} pipeline file and helper
script be created? This is most useful for multi-step, expensive, or
repeatedly rerun analyses and routes to the advanced scaffold.}

\item{use_git}{Logical. Should Git files be created and Git initialised if
available? Leave this on for most projects unless version control is being
managed elsewhere.}

\item{use_config}{Logical. Should an editable project configuration file be
created? In simple mode this creates \code{project.yml}; in advanced mode it
creates \code{config.yml}.}

\item{use_lintr}{Logical. Should linting support files be created? Enable
this when code quality checks should be part of the workflow or CI.}

\item{use_styler}{Logical. Should code formatting support files be created?
Enable this when you want a standardised code style from the start.}

\item{use_pkgdown}{Logical. Should pkgdown support files be created? This is
mainly useful for package-like projects that need a browsable documentation
site.}

\item{install_packages}{Logical. Should selected packages be installed during
project creation when possible? Turn this on for a ready-to-run scaffold;
leave it off if package installation is handled separately.}

\item{snapshot_renv}{Logical. Should \code{renv::snapshot()} be run after renv
initialisation? Only relevant when \code{use_renv = TRUE}; this records the
initial dependency state in \code{renv.lock}.}

\item{run_health_check}{Logical. Should the scaffold integrity checks run at
the end of project creation? Keeping this enabled is recommended because it
surfaces missing or skipped scaffold components immediately.}

\item{strict}{Logical. Should recoverable setup failures error instead of
being returned as warnings? This is useful for automation, CI, or any
workflow where partial scaffolds should be treated as failures.}

\item{overwrite}{Logical. Should existing files be overwritten? Leave this
off unless you intentionally want to replace an existing scaffold.}

\item{open}{Logical. Should the project be opened after creation when
possible? This flag is currently informational only; the package reports
the \code{.Rproj} file to open but does not launch it automatically.}
}
\value{
An object of class \code{"analysis_project_scaffold"}.
}
\description{
Creates a standard folder structure, helper R files, report templates,
Git-safe defaults, and optional reproducibility tooling for an R analysis
project. By default it creates a small analyst-facing scaffold with one
obvious entry point and keeps most setup machinery inside \code{projectSetupR}.
}
\details{
Raw data, processed data, generated outputs, logs, and common secret
files are ignored by Git by default when \code{use_git = TRUE}.

The default call creates a small scaffold centred on:
\itemize{
\item \code{README.md}
\item \code{run_project.R}
\item \code{project.yml}
\item \verb{analysis/}
\item \verb{reports/}
\item \verb{data/raw/}
\item \verb{data/processed/}
\item \verb{outputs/}
}

Important argument interactions:
\itemize{
\item \code{preset = "pipeline"} or \code{template = "targets"} enables advanced mode.
\item \code{preset = "package"} enables advanced mode.
\item \code{use_rmarkdown}, \code{use_targets}, \code{use_lintr}, \code{use_styler}, and
\code{use_pkgdown} enable advanced mode when needed.
\item \code{template = "quarto"} implies \code{use_quarto = TRUE}.
\item \code{snapshot_renv} matters only when \code{use_renv = TRUE}.
\item \code{packages} is required when \code{dependency_profile = "custom"}.
}
}
\section{Recommended Starting Points}{

For a lightweight analysis:
\code{create_analysis_project("my_project", preset = "analysis")}

For a modelling project:
\code{create_analysis_project("my_project", preset = "modelling")}

For a package-style scaffold:
\code{create_analysis_project("my_project", preset = "package", mode = "advanced")}
}

\examples{
tmp <- tempfile(pattern = "analysis-project-", tmpdir = tempdir())
create_analysis_project(tmp, use_renv = FALSE, use_git = FALSE, open = FALSE)
}
